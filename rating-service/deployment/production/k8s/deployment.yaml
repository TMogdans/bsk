apiVersion: apps/v1
kind: Deployment
metadata:
  name: rating-write-service
  namespace: bsk
  labels:
    app: rating-write-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: rating-write-service
  template:
    metadata:
      labels:
        app: rating-write-service
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "rating-write-service"
        dapr.io/app-port: "3000"
    spec:
      containers:
        - name: rating-write-service
          image: ghcr.io/tmogdans/bsk/rating-write-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          env:
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: rating-service-environment-config-map
                  key: PORT
            - name: APP_NAME
              valueFrom:
                configMapKeyRef:
                  name: rating-service-environment-config-map
                  key: APP_NAME
            - name: NATS_SERVER
              valueFrom:
                configMapKeyRef:
                  name: rating-service-environment-config-map
                  key: NATS_SERVER
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: rating-service-db-url
                  key: url
          resources:
            requests:
              memory: "64Mi"
              cpu: "250m"
            limits:
              memory: "128Mi"
              cpu: "500m"
      restartPolicy: Always
